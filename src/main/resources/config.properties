# Database Configuration
db.url=jdbc:mysql://localhost:3306/ftp_fs?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
db.user=root
db.password=c1s-c00p

# Optional Configuration
db.max_connections=10
db.connection_timeout=30000

# The following parameters have been added to the JDBC URL for better compatibility:
# - useSSL=false: Disables SSL for local development
# - allowPublicKeyRetrieval=true: Allows retrieving the public key from the server
# - serverTimezone=UTC: Sets the server timezone to UTC
# root
# c1s-c00p